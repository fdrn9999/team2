<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team2.householdledger.notice.mapper.NoticeMapper">

    <select id="selectNoticeList" resultType="NoticeDTO"> SELECT notice_no , user_no , message ,
        read_yn , reg_dt FROM notices WHERE user_no = #{userNo} ORDER BY read_yn ASC, notice_no DESC </select>

    <insert id="insertNotice" useGeneratedKeys="true" keyProperty="noticeNo"> INSERT INTO notices (
        user_no , message , read_yn , reg_dt ) VALUES ( #{userNo} , #{message} , 'N' , NOW() ) </insert>

    <update id="updateReadStatus"> UPDATE notices SET read_yn = 'Y' WHERE notice_no = #{noticeNo} </update>

</mapper>